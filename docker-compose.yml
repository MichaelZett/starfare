services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: starfare
      POSTGRES_USER: starfare
      POSTGRES_PASSWORD: starfare
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
